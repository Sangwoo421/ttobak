<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ttobak.backend.mapper.CounterpartyMapper">

    <sql id="columns">
        c.id, c.user_id, c.name, c.relation, c.kind, c.bank_name, c.account_number,
        c.aliases, c.memo_pattern, c.category
    </sql>

    <select id="findById" resultType="com.ttobak.backend.domain.Counterparty">
        SELECT <include refid="columns"/>
        FROM counterparties c
        WHERE c.id = #{id}
    </select>

    <select id="findByUserId" resultType="com.ttobak.backend.domain.Counterparty">
        SELECT <include refid="columns"/>
        FROM counterparties c
        WHERE c.user_id = #{userId}
        ORDER BY c.id
    </select>

    <select id="findByAccountId" resultType="com.ttobak.backend.domain.Counterparty">
        SELECT <include refid="columns"/>
        FROM counterparties c
        JOIN accounts a ON a.user_id = c.user_id
        WHERE a.id = #{accountId}
        ORDER BY c.id
    </select>

</mapper>
