<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ttobak.backend.mapper.TransactionMapper">

    <select id="findById" resultType="com.ttobak.backend.domain.Transaction">
        SELECT id, account_id, type, amount, counterparty_name, memo, channel, occurred_at, counterparty_id
        FROM transactions
        WHERE id = #{id}
    </select>

    <select id="findRecentByUser" resultType="com.ttobak.backend.domain.Transaction">
        SELECT t.id, t.account_id, t.type, t.amount, t.counterparty_name, t.memo, t.channel,
               t.occurred_at, t.counterparty_id
        FROM transactions t
                 JOIN accounts a ON a.id = t.account_id
        WHERE a.user_id = #{userId}
        ORDER BY t.occurred_at DESC, t.id DESC
        LIMIT #{limit}
    </select>

</mapper>
