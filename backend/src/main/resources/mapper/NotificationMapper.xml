<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ttobak.backend.mapper.NotificationMapper">

    <resultMap id="briefingRow" type="com.ttobak.backend.domain.BriefingRow">
        <id property="notificationId" column="notification_id"/>
        <association property="transaction" javaType="com.ttobak.backend.domain.Transaction" columnPrefix="t_">
            <id property="id" column="id"/>
            <result property="accountId" column="account_id"/>
            <result property="type" column="type"/>
            <result property="amount" column="amount"/>
            <result property="counterpartyName" column="counterparty_name"/>
            <result property="memo" column="memo"/>
            <result property="channel" column="channel"/>
            <result property="occurredAt" column="occurred_at"/>
            <result property="counterpartyId" column="counterparty_id"/>
        </association>
    </resultMap>

    <!-- 선별 규칙: heard_at IS NULL AND mute_rules 에 없는 상대 → occurred_at DESC → LIMIT -->
    <sql id="unheardWhere">
        WHERE n.user_id = #{userId}
          AND n.heard_at IS NULL
          AND NOT EXISTS (
              SELECT 1 FROM mute_rules m
              WHERE m.user_id = n.user_id AND m.counterparty_name = t.counterparty_name
          )
    </sql>

    <select id="findUnheard" resultMap="briefingRow">
        SELECT n.id                AS notification_id,
               t.id                AS t_id,
               t.account_id        AS t_account_id,
               t.type              AS t_type,
               t.amount            AS t_amount,
               t.counterparty_name AS t_counterparty_name,
               t.memo              AS t_memo,
               t.channel           AS t_channel,
               t.occurred_at       AS t_occurred_at,
               t.counterparty_id   AS t_counterparty_id
        FROM notifications n
        JOIN transactions t ON t.id = n.transaction_id
        <include refid="unheardWhere"/>
        ORDER BY t.occurred_at DESC, n.id DESC
        LIMIT #{limit}
    </select>

    <select id="countUnheard" resultType="int">
        SELECT COUNT(*)
        FROM notifications n
        JOIN transactions t ON t.id = n.transaction_id
        <include refid="unheardWhere"/>
    </select>

    <update id="markHeard">
        UPDATE notifications
        SET heard_at = NOW(),
            read_at  = COALESCE(read_at, NOW())
        WHERE id = #{id}
    </update>

</mapper>
