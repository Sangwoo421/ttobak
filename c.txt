fix: 레이어 측정이 성립하게 만들고 시연을 막던 것들 수정

가장 큰 것: baseline 경로에서도 레이어가 그대로 돌고 있어서 전후 비교가
무의미했다. 발표 7번 슬라이드의 근거가 되는 측정이다.

레이어
- mode=baseline 이면 자모 대조 대신 단순 문자열 포함으로 후보를 찾고, 되묻기도
  후보 버튼도 없이 한 번에 실패한다. LLM 의도 보정도 끄고 규칙만 쓴다.
  이전에는 힌트(레이어2)와 침묵 대기(레이어1)만 갈리고 레이어3은 양쪽 다 돌았다.
- 의도 규칙이 실패해도 LLM 앞에서 화면의 거래와 자모로 한 번 대조한다(jamo_rescue).
  "그 정보통시인가 뭐시기"는 의도 낱말도 상대명도 글자로는 안 맞지만 자모로 0.91 이다.
  여기서 구제하지 않으면 "인식이 깨져도 도달하게 한다"는 레이어의 목적이 무너진다.
  거래를 짚은 것으로만 보고, 수취인만 맞았다고 이체로 넘기지 않는다.

측정 결과 (자체 8문장, 같은 입력)
  baseline 4/8 -> layered 7/8

시연을 막던 것
- tools/reset-demo.sh 가 호스트 mysql 클라이언트를 요구하고 -p 대화형 프롬프트라
  비TTY 에서 죽었다. docker exec 로 돌리고 없으면 호스트로 폴백한다.
  리허설 두 번째부터 브리핑이 비던 원인이다.
- 시간 버킷이 백엔드(아침 05-11/오후 11-17)와 AI 서버(아침 06-12/오후 12-18)에서
  달라, 같은 거래를 브리핑은 "오늘 아침" 설명은 "오늘 오후"로 말했다. AI 서버 기준으로
  통일했다.
- 시드 시각을 NOW() 상대값에서 고정 시:분으로 바꿨다. 리셋한 시각에 따라 대본의
  "오늘 아침"이 "오늘 오후"로 바뀌던 문제. 이제 어제 오후 / 오늘 아침 / 오늘 아침 고정.

화면
- 직원 화면에서 폰 프레임 제거. 창구 단말은 노트북인데 노치 달린 430px 아이폰으로
  보였다. 대신 읽기 좋은 폭(900~1100px)으로 잡고 가운데 정렬.
- 어르신 화면 오류 문구를 한 문장으로 고정. "Network Error", "timeout of 60000ms
  exceeded", "[object Object]" 가 그대로 뜨고 있었다. 직원 화면은 상세를 유지한다.

테스트 143 passed
